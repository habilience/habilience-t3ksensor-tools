export QMAKE=~/qt-everywhere-opensource-src-5.2.0/qtbase/bin

make clean

if [ "$(uname -m)" = "x86_64" ]
then
$QMAKE/qmake T3k.pro -r -spec linux-g++-64 CONFIG+=release
else
$QMAKE/qmake T3k.pro -r -spec linux-g++ CONFIG+=release
fi
make -j8
